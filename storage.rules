rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow unauthenticated uploads ONLY to onboarding docs path
    // (users upload documents before account creation at Step 4)
    match /onboarding/employer_docs/{allPaths=**} {
      allow write: if request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('application/pdf|image/.*');
      allow read: if request.auth != null;
    }
    match /employers/{uid}/docs/{file} {
      allow read: if request.auth != null &&
        (request.auth.uid == uid || request.auth.token.admin == true);
      allow write: if request.auth != null &&
        request.auth.uid == uid && resource == null &&
        request.resource.size < 5 * 1024 * 1024 &&
        request.resource.contentType.matches('image/.*|application/pdf');
    }
    match /loans/{loanId}/{file} {
      allow read: if request.auth != null;
      allow write: if false;
    }
    match /{all=**} { allow read, write: if false; }
  }
}
