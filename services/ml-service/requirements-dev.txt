# Lint toolchain for services/ml-service. Kept out of requirements.txt so the
# runtime image does not ship a formatter.
#
# These were referenced by the pre-commit hook long before they were declared
# anywhere, which is most of why that hook never ran: nothing installed them and
# nothing said what version to install. Pinned, because an unpinned formatter
# reformats the world the day it releases a minor version.
#
# These exact versions are the ones that produced the reformat in the preceding
# commit. A different black would disagree with the tree it is checking and CI
# would be red on arrival, so these are not "latest at the time" — they are the
# versions the committed formatting was actually generated by.
black==26.5.1
flake8==7.3.0
