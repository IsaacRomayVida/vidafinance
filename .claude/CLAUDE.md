# .claude notes

- `/graphify` triggers the installed graphify skill
  (`.claude/skills/graphify/`). If that folder is missing, the skill is not
  installed in this clone yet — it lives in Isaac's Cosmoshift project and
  must be copied in from his machine.
